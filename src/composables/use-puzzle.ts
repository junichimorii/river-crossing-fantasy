// Puzzle

import { Ref, computed } from 'vue'
import { Cast } from '@/types'
interface UsePuzzleReturn {
  init: (id: number) => void
}
const usePuzzle = (
): UsePuzzleReturn => {
  const init = (
    id: number
  ): void => {
  }
  return {
    init,
  }
}
export default usePuzzle